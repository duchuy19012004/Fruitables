@model List<Fruitables.Models.Category>
@{
    ViewData["Title"] = "Thùng rác - Danh mục";
    Layout = "~/Areas/Admin/Views/Shared/_AdminDashboardLayout.cshtml";
}
<!-- Page Header -->
<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h4 class="mb-1">Thùng rác</h4>
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb mb-0">
                <li class="breadcrumb-item"><a asp-action="Index">Danh mục</a></li>
                <li class="breadcrumb-item active">Thùng rác</li>
            </ol>
        </nav>
    </div>
    <div>
        <a asp-action="Index" class="btn btn-outline-secondary">
            <i class="fas fa-arrow-left me-2"></i>Quay lại
        </a>
    </div>
</div>

<div class="admin-card">
    <div class="card-header">
        <i class="fas fa-trash me-2"></i>Danh mục đã xóa (@Model.Count)
    </div>
    <div class="card-body p-0">
        @if (Model.Any())
        {
            <div class="table-responsive">
                <table class="table table-hover mb-0">
                    <thead class="table-light">
                        <tr>
                            <th>Tên danh mục</th>
                            <th>Slug</th>
                            <th>Ngày xóa</th>
                            <th class="text-center">Thao tác</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var category in Model)
                        {
                            <tr>
                                <td>
                                    <i class="fas fa-folder text-muted me-2"></i>
                                    @category.Name
                                </td>
                                <td><code>@category.Slug</code></td>
                                <td>
                                    @if (category.DeletedAt.HasValue)
                                    {
                                        @category.DeletedAt.Value.ToString("dd/MM/yyyy HH:mm")
                                    }
                                </td>
                                <td class="text-center">
                                    <form asp-action="Restore" asp-route-id="@category.Id" method="post" style="display: inline;">
                                        @Html.AntiForgeryToken()
                                        <button type="submit" class="btn btn-sm btn-success" title="Khôi phục">
                                            <i class="fas fa-undo"></i> Khôi phục
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="text-center py-5">
                <i class="fas fa-trash fa-3x text-muted mb-3"></i>
                <p class="text-muted">Thùng rác trống</p>
                <a asp-action="Index" class="btn btn-primary">
                    <i class="fas fa-arrow-left me-2"></i>Quay lại danh sách
                </a>
            </div>
        }
    </div>
</div>
